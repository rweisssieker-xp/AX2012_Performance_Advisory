---
name: rbac-portal-generation
description: Generate role-labeled static dashboard entry points for viewer, DBA, AX admin, QA/GxP, CIO, and admin users.
---

# RBAC Portal Generation

Run `scripts/rbac_portal.py` with the dashboard HTML and output directory. Use `config/rbac.example.json` as the role model reference.
