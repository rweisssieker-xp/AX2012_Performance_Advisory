---
name: ai-query-to-ax-code-mapping
description: Map expensive SQL query evidence to AX tables and candidate X++ owners.
---

# AI Query To AX Code Mapping

Use `scripts/ai_insights.py` and read `queryToAxCodeMapping`. Use query hash, plan hash, tables, Trace Parser, and DynamicsPerf evidence. If exact X++ attribution is missing, state the required next evidence.
