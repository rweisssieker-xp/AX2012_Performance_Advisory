---
name: ai-evidence-gap-detector
description: Detect missing evidence required for confident AX/SQL diagnosis.
---

# AI Evidence Gap Detector

Use `scripts/ai_insights.py` and read `evidenceGapDetector`. Convert each gap into a read-only next collection step, such as deadlock XML, Trace Parser, DynamicsPerf, Query Store, batch history, or session evidence.
