---
name: optional-agent-operations
description: Configure, dry-run, or operate the optional AXPA local agent and scheduled task wrapper.
---

# Optional Agent Operations

Use `scripts/optional_agent.py` and `scripts/install_optional_agent.ps1`. The agent is optional and not required for plugin use. Prefer `-DryRun` before registering a scheduled task.
