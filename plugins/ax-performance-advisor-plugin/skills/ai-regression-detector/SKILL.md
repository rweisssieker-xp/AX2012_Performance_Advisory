---
name: ai-regression-detector
description: Identify performance regressions after deployments, configuration, index, or schedule changes.
---

# AI Regression Detector

Use `scripts/ai_insights.py` and read `regressionDetector`. Compare before/after evidence, Query Store runtime, plan-cache variance, and deployment metadata. Mark candidates separately from proven regressions.
