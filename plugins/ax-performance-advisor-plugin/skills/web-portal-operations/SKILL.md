---
name: web-portal-operations
description: Configure, validate, or run the local AXPA RBAC web portal with token-based dashboard access and audit logging.
---

# Web Portal Operations

Use `scripts/web_portal.py` with `config/web-portal.example.json`. Prefer `--check` first. Treat token auth as local/internal only unless deployed behind a real identity provider.
